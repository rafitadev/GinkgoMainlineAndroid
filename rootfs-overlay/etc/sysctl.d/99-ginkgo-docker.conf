# Docker bridge NAT / overlay. Engine also sets these; persist them so
# container networking works even before dockerd starts.
net.ipv4.ip_forward = 1
net.ipv6.conf.all.forwarding = 1
net.ipv6.conf.default.forwarding = 1
